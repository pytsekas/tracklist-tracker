# One container. The SQLite archive is baked into the image at build time, so
# there is no database service, no volume and nothing to wait for.
services:
  app:
    build: .
    image: tracklist-browser:local
    restart: unless-stopped
    environment:
      PORT: 3000
    ports:
      - "${APP_PORT:-3000}:3000"
